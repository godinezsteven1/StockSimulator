package controller;

/**
 * This interface represents all the operations to be offered by the APIController.
 * These operations are supposed to be a barebones set upon which other operations
 * may be developed.
 */
public interface APIController {

  /**
   * Runs the program's features of seeing and using stock.
   */
  public void execute();
}
